{
  "name": "dice-ui-react",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "babel": "babel --watch src/ --out-dir build/",
    "test": "mocha --compilers js:babel-core/register",
    "clean": "rm -rf dist",
    "build": "mkdir -p dist; ./node_modules/.bin/browserify -o dist/gameOfLife.js -r ./build/game.js -s game"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2",
    "browserify": "^13.1.1",
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
